export enum QUEUE {
  PROCESSORS = 'processor',
}

export enum QUEUE_STATUS {
  WAITING = 'waiting',
  ACTIVE = 'active',
  COMPLETED = 'completed',
  FAILED = 'failed',
  DELAYED = 'delayed',
  PAUSED = 'paused',
}
